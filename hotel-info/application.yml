#config properrties of hotel info services follows

spring:
  application:
    name: eureka-registry
    
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
        defaultZone: http://localhost:8761/eureka      
